; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
default_envs = esp32-c3-supermini

[env:esp32-c3-supermini]
; Pinned because this implementation uses the Arduino-ESP32 2.x legacy RMT API.
platform = espressif32@7.0.0
; The common 4 MB ESP32-C3 SuperMini is compatible with this target.
board = esp32-c3-devkitm-1
framework = arduino
monitor_speed = 115200

; SuperMini USB-C is connected to the ESP32-C3 USB Serial/JTAG peripheral.
; Debug Serial therefore does not occupy CRSF GPIO0/GPIO1.
build_flags =
    -D ARDUINO_USB_MODE=1
    -D ARDUINO_USB_CDC_ON_BOOT=1

[env:native]
platform = native
test_framework = unity
build_flags = -std=c++17
